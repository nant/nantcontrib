<?xml version="1.0"?>
<project name="nant" default="nightly">
    
    <script language="C#">
        <code>
			<![CDATA[
			public static void ScriptMain(Project project) {
				project.Properties.AddReadOnly("project.version", DateTime.Now.ToString("yyyyMMdd"));
			}
			]]>
		</code>
    </script>
    
    <property name="sf.net.ssh.server" value="shell1.sf.net"/>
    <property name="sf.net.build.path" value="/home/groups/n/na/nantcontrib/htdocs/builds"/>
    
    <include buildfile="nantcontrib.build"/>
    <!-- do a release build -->
    <property name="debug" value="false"/>
    
    <target name="nightly" depends="build userdoc doc" description="Perform a 'nightly' package, and updates the version of NAnt that is executing.">
        <property name="project.zip-path" value="${nant.project.basedir}/build/${project.name}-${project.version}.zip" />

        <zip zipfile="${project.zip-path}">
            <fileset basedir="${build.dir}">
                <includes name="**/*" />
                
                <excludes name="**/cache/**"/>
                <excludes name="**/_*/**"/>
                <excludes name="**/Schemas/**"/>
                <excludes name="**/docs/**"/>
            </fileset>
        </zip>

		
		<!-- copy the build up to sourceforge -->
		<scp file="${project.zip-path}" server="${sf.net.ssh.server}" path="${sf.net.build.path}"/>
		<echo message="Copied nightly build over to SF.Net"/>		
		
		<!-- make sure files are readable -->
		<exec program="ssh" failonerror="false">
			<arg value="${sf.net.ssh.server}"/>
			<arg value="chmod -R 755 ${sf.net.build.path}/*"/>
		</exec>
    </target>
    
</project>